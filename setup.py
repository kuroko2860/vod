from setuptools import setup, find_packages

setup(name="videood",
      version='1.0',
      description='A python library built to detect objects in videos',
      author='kuroko',
      license='MIT',
      packages=find_packages(),
      install_requires=[],
      include_package_data=True,
      zip_safe=False)
